-- Exercise
-- Order of execution

-- Instructions
-- Drop the SQL keywords into the order that they will be executed in (not the written order) from first (top) to last (bottom).
limit
from
select
-- MY SOLUTION:
from
select
limit
